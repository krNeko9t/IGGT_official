# --- 核心深度学习 (假设 torch 已手动安装) ---
# torchvision>=0.20.0
# torchaudio>=2.5.0
accelerate>=1.10.1
diffusers>=0.35.1
transformers>=4.46.0  # 稍微放宽版本
timm
# xformers>=0.0.28      # 注意：ARM64 上 xformers 极难编译，如果报错且非必须，建议注释掉

# --- 3D 与 视觉常用 ---
open3d>=0.18.0        # ARM64 编译慢，耐心等待
trimesh
opencv-python
pillow
matplotlib
scikit-image
imageio
viser>=0.2.23         # 可视化工具
wandb                 # 实验记录

# --- MMLab 系列 (文件里有) ---
# 注意：在 ARM 上建议用 openmim 安装这些，直接 pip 容易挂
# mmcv==1.6.0         # 这是一个很老的版本，可能与 torch 2.5 不兼容，需确认代码兼容性
# mmengine

# --- 图与工具 ---
# torch-geometric>=2.6.1 # 文件里有，注意依赖
# torch-scatter
# torch-sparse
networkx
scipy
numpy<2.0              # 很多旧代码不兼容 numpy 2.0，建议先保守一点，虽然原文件是 2.0.2

# --- 其他杂项 ---
tqdm
pyyaml
rich
hydra-core
packaging
ninja
tyro

gradio==5.17.1
gradio_client==1.7.1
evo==1.31.1
scikit-learn==1.6.1
hdbscan==0.8.40
einops
basicsr==1.4.2
apex==0.1